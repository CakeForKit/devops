ssh -p 2222 kate@192.168.170.138
ssh -p 2222 kate@192.168.170.138


# ssh vm1
ssh kate@192.168.75.2

CADDY 
/mnt/hgfs/devops/run.sh


NGINX
sudo apt install nginx -y
sudo nano /etc/nginx/sites-available/default

proxy_pass http://192.168.75.2:80


# Поднять интерфейс вручную
ip a
sudo ip link set ens33 up
sudo netplan apply


Настроить шлюз через netplan

Временная настройка (для проверки)
# На ВМ2
sudo ip route add default via 192.168.75.1

Постоянная:
mv /etc/netplan/50-cloud-init.yaml /etc/netplan/50-cloud-init.yaml-backup-$(date +"%Y%m%d")

Чтобы cloud-init больше не создавал свой файл после перезагрузки сервера, создайте файл /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg: nano /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg
И пропишите в нем эту строку: network: {config: disabled}

sudo nano /etc/netplan/99-config.yaml
yaml
network:
  ethernets:
    ens33:
      addresses:
        - 192.168.75.2/24
      routes:
        - to: default
          via: 192.168.75.129    
      nameservers:
        addresses: [8.8.8.8, 1.1.1.1]
  version: 2




форвардинг 
sudo sysctl -w net.ipv4.ip_forward=1

Сделать постоянным
sudo nano /etc/sysctl.conf

sudo iptables -t nat -A POSTROUTING -o ens33 -j MASQUERADE

sudo iptables -A FORWARD -i ens37 -o ens33 -j ACCEPT
sudo iptables -A FORWARD -i ens33 -o ens37 -m state --state ESTABLISHED,RELATED -j ACCEPT


sudo apt install iptables-persistent -y
sudo netfilter-persistent save








server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/my_app;
    index index.html index.htm;

    server_name _;

    location / {
        proxy_pass {{ proxy_path }};
    }
}